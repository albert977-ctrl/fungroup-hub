/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Uncomment to enable experimental features if needed
  // experimental: {
  //   esmExternals: true,
  // },
};

export default nextConfig;
